<ul class="nav navbar-nav navbar-right">

@if (Request.IsAuthenticated)
{
    
     <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Xin chào @User.Identity.Name <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li><a href="@Url.Action("Index","UserQuery")">Tin đã lưu</a></li>
                            <li class="divider"></li>
                            <li><a href="javascript:document.getElementById('LogOutForm').submit()" class="">Đăng xuất</a></li>
                            
                        </ul>
                    </li>
    using (Html.BeginForm("LogOut", "Account", FormMethod.Post, new { id = "LogOutForm", @class = "form-inline" }))
    {
        @Html.AntiForgeryToken()
    }
}
else
{
    <li>@Html.ActionLink("Đăng nhập", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
}
</ul>
